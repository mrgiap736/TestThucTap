@model IEnumerable<App_Data.Entities.Staff>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                STT
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Code)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccountFE)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.AccountFPT)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @{
            int stt = 0;
            foreach (var item in Model) {

                stt++;
        <tr>
            <td>
                @stt
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Code)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccountFE)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AccountFPT)
            </td>
                    @{
                        if (item.Status == 1)
                        {
                            <td>Đang làm việc</td>
                        }
                        else if(item.Status == 0)
                        {
                            <td>Đã nghỉ việc</td>
                        }
                    }
            <td>
                        <a asp-action="Edit" asp-route-id="@item.Id"><i class="fa-regular fa-pen-to-square"></i></a> |
                        <a asp-action="ChangeStatus" asp-route-id="@item.Id"><i class="fa-solid fa-rotate"></i></a> |
                        <a asp-action="Details" asp-route-id="@item.Id"><i class="fa-regular fa-eye"></i></a>
            </td>
        </tr>
    }
}
    </tbody>
</table>

<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.6.0/css/all.min.css" integrity="sha512-Kc323vGBEqzTmouAECnVceyQqyqdsSiqLQISBL29aUW4U/M7pSPA/gEUZQqv1cwx4OnYxTxve5UMg5GT6L4JJg==" crossorigin="anonymous" referrerpolicy="no-referrer" />
